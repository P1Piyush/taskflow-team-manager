[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'cd /app/server && npx prisma migrate deploy && node dist/index.js'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "web"
